import { Component, Vec3, _decorator } from "cc";
const { ccclass, property } = _decorator;

@ccclass("Map ")
export class Map extends Component {

    /**
     * Check if the map is reach able
     * @param p 
     */
    checkReachable(p:Vec3){

    }

}